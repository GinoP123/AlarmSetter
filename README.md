# Alarm Setter Daemon Using Crontab

Usage:
- `a <time (minutes)>`: Set new alarm in <time> minutes.
- `ac`: Check when next alarm is.
- `unset_alarm`: Remove next alarm.
- `aa`: Start pomodoro timer with 2 blocks.